import { PermissionsLabelPipe } from './permissions.label.pipe';

describe('PermissionsLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionsLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
