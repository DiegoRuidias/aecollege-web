import { LabelBlankUsersPipe } from './label-blank-users.pipe';

describe('LabelBlankUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelBlankUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
