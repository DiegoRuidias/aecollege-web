import { PeriodoTagPipe } from './periodo.tag.pipe';

describe('PeriodoTagPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodoTagPipe();
    expect(pipe).toBeTruthy();
  });
});
